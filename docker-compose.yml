version: '3.8'

services:
  backend:
    build:
      context: ./back-end
      dockerfile: Dockerfile
    container_name: spark_backend
    ports:
      - "8000:8000"
    env_file:
      - ./back-end/.env
    volumes:
      - ./back-end_data:/app/databases
    restart: always
